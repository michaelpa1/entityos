# Database (server-only — never expose to the client)
DATABASE_URL=

# Public app URL (used for metadata; safe to expose)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional site password for production MVP (server-only; not exposed to client)
# On Vercel Hobby, use this when Advanced Deployment Protection is unavailable.
SITE_PASSWORD=

# Cron + AI snapshot automation (server-only)
CRON_SECRET=
OPENAI_API_KEY=
GEMINI_API_KEY=
GOOGLE_SEARCH_CONSOLE_SITE_URL=
GOOGLE_SERVICE_ACCOUNT_JSON=
# Legacy aliases still supported by the runner:
# OPTIONAL_OPENAI_API_KEY=
# OPTIONAL_GEMINI_API_KEY=
